📋 数据分析器提示词（建议直接复制以下全部内容）

**【角色设定】**

你现在是一位资深的制造业质量数据分析师及BI可视化专家。请读取我上传的Excel数据文件，并按照以下要求进行数据清洗、特殊逻辑计算，并输出多维度的分析报告与可视化图表，用html文件展示。

**【核心数据处理规则：有效工作时长计算】**

在进行“处理时长”、“平均响应时间”等相关计算时，**必须严格扣除非工作时间**，只计算有效工作小时数。具体逻辑如下：

1. **每日工作时间**：上午 `08:30-12:00`，下午 `13:30-17:30`（每天共计7.5小时）。
2. **需扣除的时间段**：
   - 午休时间（每天的 `12:00-13:30`）。
   - 夜间非工作时间（每天的 `17:30` 至次日 `08:30`）。
   - 周末（周六、周日）及常规法定节假日全天时间不计入。
3. 请编写专门的Python函数（如利用 `pandas` 和 `business-duration` 或自定义时间过滤逻辑），精准计算每个工单从“提报时间”到“验收时间/闭环时间”的【有效工作耗时（小时）】。

**【分析与可视化输出要求】**

请基于清洗后（并包含计算好的有效耗时）的数据，完成以下7大板块的分析，并为每个板块生成对应的结论与图表代码/图片：

- **第一板块：全局概览与趋势**
  - 计算并打印核心KPI：总异常数、未闭环数、平均处理耗时（有效工作小时）、内外责任占比。
  - 绘制每日提报趋势折线图。
  - 绘制单据处理状态分布的漏斗图或饼图。
- **第二板块：痛点对象定位**
  - 生成各项目异常数量的饼图。
  - 找出故障频发列车 Top 15，绘制水平条形图。
  - 用矩形树图分析异常多发产品构型。
  - 绘制核心项目与构型的交叉热力图。
- **第三板块：缺陷特征剖析**
  - 用南丁格尔玫瑰图展示现象分类占比。
  - 用帕累托图（二八定律）展示导致80%故障的核心失效模式。
  - 如果数据支持，用桑基图展示“现象分类 -> 失效模式 -> 失效原因”的演变路径。
- **第四板块：责任归属与追溯**
  - 对比内外（制造/供应商/设计等）责任比例。
  - 绘制责任单位/班组缺陷红黑榜（柱状图）。
  - 用堆叠柱状图展示“责任单位 vs 失效原因”的交叉分析。
- **第五板块：流程效率与时效评估**
  - 绘制异常处理有效耗时的区间分布（直方图）。
  - 用散点图分析各班组的时效对标（X轴为工单量，Y轴为有效平均耗时），找出低效节点。
- **第六板块：整改方案与经验挖掘**
  - 针对初步诊断、现象详情、整改方案文本列进行分词清洗，提取高频词并生成词云。对比“现象”与“整改”的关键词差异。
- **第七板块：人员与空间质量红黑榜**
  - 按总数倒序排列责任人 Top 10，绘制内部按失效模式分层堆叠的柱状图。
  - 尝试分析物理空间/车厢节号与缺陷密度的关系，提示高发病区。

**【执行要求】**

1. 请先输出数据的基础信息（`.info()`和`.head()`），检查字段缺失情况。
2. 请展示您用于计算【有效工作时长】的核心Python函数代码。
3. 逐步执行各个板块的分析，每完成一个板块，请给出一段简练的“业务洞察结论”。